# Clinica Estoque

Sistema simples de controle de estoque utilizando React e Supabase.

## Instalação

```bash
npm install
npm start
```

Crie um arquivo `.env` baseado em `.env.example` com as credenciais do Supabase.
